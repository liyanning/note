这次修改在类上面引用了两个类，分别是CircleButton类还有CircleImageView类，这两个类都有定义一些attrs的资源，这些都写在了attrs.xml里面了。

然后关于layout方面的改动：
（1）改动了nav_header_diary_list（就是那个用户头像的那个蓝色框）
（2）改动了content_diray_list（把左上右的padding去掉了）
（3）改动了diary_item（把（2）中的padding移动到了diary_item中）